1. Download weigths for each model, label them and put them in the weigths folder
2. run install.py to download the datasets from kaggle (on local fs)
3. (OPTIONAL) run get_info.py to get the info from the model and datasets
4. run main.ipynb
